export const supabaseErrors: any = {
	'User already registered': 'Usuario ya registrado',
	'Invalid login credentials': 'Credenciales de acceso inválidos',
}


export interface IColors {
  [key: string]: string;
}
export const colors: IColors = {
  azul: "bg-blue-400",
  "azul oscuro": "bg-blue-950",
  negro: "bg-black",
};
